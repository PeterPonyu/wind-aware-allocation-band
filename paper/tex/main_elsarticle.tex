%% Generated by scripts/venue_skin.py from the release ledger.
%% Do not edit: the prose lives in body.tex, which the default binding
%% (main.tex) includes as well, so the two can never say different things.
%% Regenerate with: python3 scripts/venue_skin.py --emit <paper dir>
%% Venue state: PROPOSED. A proposed venue is a plan, not an acceptance.
\documentclass[3p,11pt,number,nopreprintline]{elsarticle}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{array}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{tikz}
\usepackage{placeins}
% For every included figure, pdfTeX records the absolute path it was
% read from in a /PTEX.FileName object.  Those objects are not
% rendered, so a text dump of the PDF does not show them, but they
% travel with the file and spell out the build machine's home
% directory, user name and worktree layout.
\pdfsuppressptexinfo=-1

\usepackage[hidelinks]{hyperref}
% Blind copies are compiled with \ANONYMOUS defined; the identified
% build is the default.  hyperref writes the name into the PDF /Author
% key as well as the title page, so both go through this one macro.
\ifdefined\ANONYMOUS\newcommand{\PDFAUTHOR}{}\else\newcommand{\PDFAUTHOR}{Zeyu Fu}\fi
\hypersetup{pdftitle={From directional sensing to task completion: the operating envelope of target-position feedforward against a slow integral in wind-disturbed quadrotor waypoint capture},pdfauthor={\PDFAUTHOR},pdfsubject={Operating envelope of target-position feedforward against a slow integral},pdfkeywords={disturbance feedforward, integral control, wind estimation, thresholded endpoint, task allocation, quadrotor swarm, reproducibility}}
\numberwithin{equation}{section}

\graphicspath{{../figs/out/}}
\setlength{\emergencystretch}{2em}

\renewcommand{\topfraction}{0.9}
\renewcommand{\bottomfraction}{0.7}
\renewcommand{\textfraction}{0.08}
\renewcommand{\floatpagefraction}{0.8}
\setcounter{topnumber}{2}
\setcounter{totalnumber}{3}
\setlength{\bibsep}{0pt plus 0.3ex}
\myfooterfont{\scriptsize\itshape}

\input{generated_numbers}
\input{captions}
\input{theorems}
\newcommand{\generatedtable}[1]{\resizebox{\linewidth}{!}{\input{#1}}}

\journal{Robotics and Autonomous Systems}

\begin{document}

\begin{frontmatter}

\title{From directional sensing to task completion: the operating envelope of
target-position feedforward against a slow integral in wind-disturbed quadrotor
waypoint capture}

\ifdefined\ANONYMOUS
% No \address and no [amu] key: elsarticle prints a superscript
% affiliation marker for every keyed author, so an empty keyed author
% leaves a dangling marker and an empty entry in the /Author key.
\author{Author and affiliation withheld for blind review}
\else
\author[amu]{Zeyu Fu\corref{cor1}}
\ead{fuzeyu99@126.com}
\cortext[cor1]{Corresponding author. ORCID: 0009-0001-8329-0108.}
\address[amu]{Army Medical University, Chongqing, China}
\fi

\begin{abstract}
\input{abstract}
\end{abstract}

\begin{keyword}
disturbance feedforward \sep integral control \sep wind estimation \sep thresholded endpoint \sep task allocation \sep quadrotor swarm \sep reproducibility
\end{keyword}

\end{frontmatter}

\hypersetup{pdfsubject={Operating envelope of target-position feedforward against a slow integral},pdfkeywords={disturbance feedforward, integral control, wind estimation, thresholded endpoint, task allocation, quadrotor swarm, reproducibility}}
\input{body}

{\footnotesize
\bibliographystyle{elsarticle-num}
\bibliography{references}
}

\end{document}
